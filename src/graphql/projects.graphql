query Projects($where: Project_filter, $first: Int, $skip: Int, $orderBy: Project_orderBy, $orderDirection: OrderDirection) {
  projects(where: $where, first: $first, skip: $skip, orderBy: $orderBy, orderDirection: $orderDirection) {
    projectId
    metadataUri
    handle
    contributorsCount
    createdAt
  }
}